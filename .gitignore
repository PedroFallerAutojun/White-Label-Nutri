__pycache__/
*.pyc
.venv/
venv/
.env
db.sqlite3
staticfiles/
media/
.pytest_cache/
.coverage

# Dumps e despejos de banco nunca entram no repositório (dados de cliente).
# A pasta inteira é ignorada porque os backups do Heroku vêm SEM extensão
# (ex.: "BackupNutriJR"), e as regras por extensão não os pegariam.
backups/*
!backups/LEIA-ME.md
*.sql
*.dump
*.dump.gz
*.backup
